# État du projet Timer : planning journalier

> Tenu à jour à chaque modification, comme les `ETAT_PROJET.md` de
> `Comm distance/`.

## État

**Moteur livré et testé** (19 tests), **codec JSON** du document (13 tests :
le même schéma servira l'API et la persistance §3.3), et **banc local de
l'API** : le vrai moteur derrière les routes de la spec §6, horloge simulée
pilotable, IHM avec bandeau de validation, 10 tests de contrat. Restent le
matériel (DS3231), la couche fichier et le portage ESP32.

## Décisions actées

| Date | Décision |
|---|---|
| 2026-08-27 | DST printemps : **exécution au premier instant existant** (03:00), décalage signalé. Le saut reste configurable. |
| 2026-08-27 | Fenêtre de grâce par défaut : **5 min** (suggestion spec §4.2 adoptée). |
| 2026-08-27 | Versionnage : **dépôt unique à la racine `AGV_MEIDEN/`**, `.git` remonté depuis `Comm distance/` (historique conservé). |

## Kanban

| # | Tâche | Note |
|---|---|---|
| T1 | ⚠️ **Persister les clés consommées** avec le planning (§3.3) | Sans cela, un redémarrage **dans la fenêtre de grâce** rejoue une mission déjà partie, et le journal de boot peut mentir sur ce qui a réellement tourné |
| T2 | Persistance : **`NvsStore` suffit**, pas besoin de LittleFS, le document fait 1–2 ko | Le **codec JSON existe** ; l'écriture atomique est assurée par la NVS |
| T3a | ⚠️ **SNTP + fuseau + état de confiance**, `set_wall_clock()` existe et **n'est appelé nulle part** : l'heure vaut 0, le moteur est gelé | **Le seul vrai préalable** sur V5.0.1 |
| T3b | Pilote DS3231 (`IO21`/`IO22`, libres) | Le Wi-Fi du site est saturé : sans lui, un AGV redémarré sans réseau **ne part pas** |
| T4 | **Routes REST sur l'interface CLIENTE**, en permanence + **authentification** | Le serveur web n'existe aujourd'hui que dans la fenêtre de maintenance (600 s, ILS). C'est la permanence qui impose l'authentification |
| T5 | Bandeau de validation + rappel actif 06:30 (§6) | Non contournable |
| T6 | Intégration A3 : mission → `Cmd::Goto` sur la liaison existante | Le séquenceur §5 est déjà écrit et testé, voir `ALIGNEMENT_COMM_DISTANCE.md` |
| T7 | **Instrumentation du tas (§7.4), à faire tôt** ; reboot quotidien ensuite | Le moteur alloue (`std::string`, `std::vector`) ; le reboot **masquerait** une fuite au lieu de la corriger |
| T8 | Sélecteur physique « autorisation planning » (impact **nomenclature**) | Verrou d'**exploitation** : maintenance, nettoyage. Pas un organe de sécurité, voir `DEPLOY.md` §0.1 |
| T9 | Mesure physique de `t_setup` | Déjà au kanban de A3, même mesure |
| T10 | ⚠️ **Sauvegarder le firmware d'origine de l'ESP32** avant tout flash | Bloque le banc sur carte (`banc_api/esp32/DEPLOY.md` §1). Cinq ans de production, aucune sauvegarde : décision client par écrit si la flash est protégée en lecture |
| T11 | Chaîne de compilation ESP32 sur le poste de développement | Ni `pio`, ni `esptool`, ni `xtensa-esp32-elf-gcc`. `src/main.cpp` du banc sur carte n'a jamais été compilé |

## Journal

| Date | Modification | Impact |
|---|---|---|
| 2026-08-28 | **Banc de l'API sur la carte** ajouté (`banc_api/esp32/`) : l'ESP32 de la V5.0.1 ouvre un point d'accès `agv-atelier` et sert l'IHM sur `http://192.168.4.1/`. Ce n'est **pas** un changement de cible, le Timer reste destiné au poste central : c'est un banc, qui mesure ce que le banc local ne peut pas voir (pile IP embarquée, tenue mémoire sur 320 Ko, plusieurs clients). Le serveur n'est **pas recopié** : `platformio.ini` compile le même `banc_api/serveur.cpp`, deux points seulement divergent sous `ESP_PLATFORM`, les pages web (embarquées par `outils/embarquer_web.py`, faute de système de fichiers) et l'adresse d'écoute. Décision : sur carte, `heure_fiable` démarre à **`false`**, une carte nue démarre en 1970 et un point d'accès sans internet n'a pas de NTP, le moteur doit donc rester gelé jusqu'à `POST /api/sim/heure` plutôt que tirer les départs du 1er janvier 1970. ⚠️ **Non déployé** : aucune chaîne ESP32 sur la machine (ni `pio`, ni `esptool`), et surtout **aucune sauvegarde du firmware d'origine**, que la carte porte depuis cinq ans, le `read_flash` est un préalable non négociable. Vérifié sans carte : les branches `ESP_PLATFORM` compilent en `-Werror`, lwIP fournissant les mêmes appels que POSIX ; `src/main.cpp` n'est couvert par rien. | banc, serveur, DEPLOY |
| 2026-08-28 | **Le Timer change de cible : il tourne sur le poste central**, sous Linux, et non plus sur l'ESP32 embarqué. Trois des quatre obstacles de l'analyse précédente disparaissent : NTP standard remplace le SNTP à écrire, le système de fichiers remplace la NVS, et le serveur tourne en permanence au lieu d'une fenêtre de 600 s. **Le banc devient la cible** : `banc_api/serveur.cpp` est du C++ pur sur sockets POSIX, il tourne tel quel sur un Raspberry Pi. Restent quatre points : écouter sur le réseau, horloge système à la place de la simulée, persistance fichier, authentification. `CHEMIN_V5.md` devient [`CHEMIN_POSTE.md`](CHEMIN_POSTE.md) ; les phases 2 à 4 de `DEPLOY.md` sont marquées caduques. Les bancs LoRa Linux et EnOcean deviennent la base du transport et de la réception d'appels du poste. | cible, DEPLOY, README |
| 2026-08-27 | **Tirets cadratins retirés de tout le dépôt** sur demande du client : 2318 occurrences, 360 fichiers, documentation, commentaires de code, chaînes affichées et gabarits web. Remplacement **contextuel** plutôt qu'uniforme : deux-points quand la suite explique, virgule après une liaison (*et, mais, donc, dont, notamment*), parenthèses pour une incise complète, trait d'union simple pour une valeur absente dans un tableau. Deux défauts corrigés au passage : le générateur produisait « Nomenclature : A3 : LoRa » (double ponctuation, le titre portant déjà son code), et le libellé de plage « Numérotation A1-A3 » avait vu sa borne basse renumérotée par mes permutations successives A1/A2 puis A2/A3. Contrôles passés : fidélité `agvdump`, conformité du thème, 470 + 53 + 19 tests. | tout le dépôt |
| 2026-08-27 | **`DEPLOY.md` recalé sur l'analyse V5.0.1**, il était resté sur l'ancienne, alors que `CHEMIN_V5.md` et le kanban avaient bougé : c'est la divergence que la règle de tenue documentaire existe pour éviter, et elle m'a échappé. Corrigé : la phase 0 ne prétend plus que tout y est éliminatoire (seul `t_setup` l'est), §0.4 reprend sa place après §0.3, la **phase 2 se scinde en SNTP puis DS3231**, SNTP d'abord, `set_wall_clock()` n'étant appelé nulle part -, la phase 3 passe de **LittleFS à `NvsStore`**, et la phase 4 devient « l'IHM joignable en permanence » avec ce qu'elle impose. La phase 5 liste les trois branchements, dont `en_deplacement`. Deux essais ajoutés à la recette : **réseau coupé avec DS3231 en place** (les départs doivent avoir lieu, c'est ce que le composant achète) et deux lignes de diagnostic, dont `/api/time` à l'epoch 1970. | DEPLOY, README |
| 2026-08-27 | **Chemin de déploiement sur la V5.0.1 établi** (devenu [`CHEMIN_POSTE.md`](CHEMIN_POSTE.md)). Le choix de cette carte (celle de A3, donc avec Wi-Fi) change l'ordre des priorités. Constat central : **`set_wall_clock()` existe et n'est appelé nulle part**, l'horloge murale vaut donc 0 en permanence et le moteur reste gelé : **SNTP est le seul vrai préalable**. Le DS3231 reste nécessaire mais pour une raison propre au site, le Wi-Fi 2,4 GHz y est saturé, un AGV redémarré sans réseau n'aurait pas d'heure et ne partirait pas. Trois raccourcis identifiés : **`NvsStore` remplace LittleFS** (document de 1–2 ko, écriture atomique déjà assurée), le **séquenceur et le transport existent** (rien à écrire du bus), et le **contrat REST est déjà figé par le banc**. Un besoin découvert : le serveur web ne tourne que dans la **fenêtre de maintenance** de 600 s, c'est sa mise en permanence qui impose l'authentification, pas le déclenchement autonome. Encombrement mesuré : ~27 ko de code. Kanban recalé. | kanban, DEPLOY |
| 2026-08-27 | **Arrêt de sécurité rapporté par le planning**, et §0.1 du DEPLOY **corrigé**. Le client rappelle que l'AGV porte sa propre chaîne, indépendante : elle coupe la puissance sur obstacle et exige un réarmement physique. Ma phase 0 présentait donc à tort la sécurité comme **éliminatoire** sur un scénario de heurt, surestimé, la chaîne le traite. Ce qui change réellement avec le déclenchement autonome est la **surprise** et surtout l'**indisponibilité** (arrêt à 06:00 sans personne pour réarmer) ; le sélecteur physique reste recommandé comme verrou d'**exploitation**, pas comme organe de sécurité. Le moteur gagne `interruption_agv(en_deplacement)` : **coupure pendant un trajet → alerte nommée** (départ, destination, heure) qui **suspend les départs autonomes** jusqu'à acquittement, **coupure à l'arrêt → journal seulement**, crier au loup à chaque coupure viderait l'alerte de son sens. L'appel opérateur reste permis pendant l'alerte : décision humaine. 5 tests, bandeau ⛔ dans l'IHM, simulation `/api/sim/interruption`. ⚠️ Le firmware devra fournir `en_deplacement` depuis `SeqState::Transit` + `kMoving` (T6). | moteur, API, IHM, DEPLOY |
| 2026-08-27 | **Thème unifié sur toutes les interfaces du dépôt**, fond blanc, texte noir, bleu outremer, logo AIO : les six IHM (4 supervisions d'architecture, banc EnOcean, planning) partageaient jusqu'ici **trois palettes différentes**, deux sombres et une claire. La feuille vit une seule fois dans `docs/theme/theme.css` ; chaque interface en porte une copie (dossiers livrables seuls, et le banc part en `rsync` sur une UniPi). `outils/theme.sh` propage l'original et **signale les copies qui ont dérivé**, même garde que pour le rendu `agvdump`. Les `style.css` ne gardent que le spécifique. Deux défauts trouvés au rendu : le serveur du banc planning servait **tout** en `text/html`, donc la feuille était ignorée ; et les notifications recouvraient l'en-tête. Le banc EnOcean passe en **v1.2.0** pour que le contrôle de version du déploiement signale une copie périmée. | 6 interfaces, thème, outils |
| 2026-08-27 | **La page `agvdump` entre dans l'IHM du planning**, derrière une barre d'onglets `agvschedule` / `agvdump`, sur la cible, les deux seront servies par le même serveur ESP32. ⚠️ **Le format n'est pas modernisé** : `GET /agvdump` reste du texte brut identique au firmware d'origine (§3.3), les procédures d'atelier le lisent ; c'est `agvdump.html` qui le met en forme, en allant chercher ce même texte. Le rendu est une **copie octet pour octet** de celui de A3, et `test/test_agvdump.cpp` **compare la copie à son original** à chaque build, une copie sans ce contrôle est une divergence en sursis. Trois défauts vus à la capture et corrigés : titre dupliqué, colonnes écartées sur toute la largeur (`table{width:100%}` du thème), et `uptime_s` qui suivait l'horloge **simulée**, un saut au mois prochain se lisait comme des semaines de fonctionnement. | IHM, atelier, 48 tests |
| 2026-08-27 | **La frise saturait en texte** : sa hauteur et ses trois rangées étaient figées dans la feuille de style. Toute la géométrie verticale passe dans un objet `GEO` du script, et **la hauteur suit les rangées réellement occupées** (jusqu'à dix, plancher à trois). Vérifié sur une journée chargée : **onze départs sur quatre rangées, sans chevauchement**. Ordre de peinture rendu explicite (`z-index` occupation < étiquette < curseur), sans lui, l'ordre du DOM décidait et le bloc d'occupation recouvrait l'heure du curseur. | IHM du banc |
| 2026-08-27 | **La frise mentait sur l'occupation de l'AGV.** L'étiquette d'un départ mesurait ~92 px sur une frise de 1030 px, soit **2,1 h d'immobilisation apparente** pour un trajet qui en dure **5 min** (0,35 %, soit 3,6 px). Corrigé en séparant deux objets : un **bloc d'occupation à l'échelle réelle sur l'axe**, et une **étiquette détachée** reliée par une amorce d'un pixel, qui n'engage aucune durée. La durée devient `Config::duree_mission_s` (défaut 300 s), exposée par `/api/time`, une seule source de vérité pour le moteur et l'IHM, pas une constante de CSS. Le moteur marque désormais les **chevauchements** (`Prochaine::conflit`, test dédié), l'API les expose, l'IHM les cerne d'ambre, et **poser un départ à moins de 5 min d'un autre est refusé à la saisie** : le séquenceur refusant d'empiler sans accusé (§5), le départ serait perdu. Placement des étiquettes sur trois rangées **par détection de chevauchement** au lieu d'une alternance aveugle. | moteur, API, IHM |
| 2026-08-27 | **Deux défauts de rendu corrigés sur l'IHM, constatés par l'exploitant.** (1) Drapeaux invisibles : `.fanion` posait `color:#fff` **et** `background:currentColor`, `currentColor` se résout contre la couleur de l'élément lui-même, d'où un fanion blanc-sur-blanc, seul le mât de 2 px restait. La couleur d'état vit sur `.drapeau`, le blanc du texte sur le `span`. (2) `rendrePostes()` n'était **jamais appelé au démarrage** : aucune pastille, donc aucun geste possible. Rendu synchrone avant le premier échange réseau. **La vérification a changé de nature** : capture Firefox headless d'une page peuplée, via une page-enveloppe au `load` retardé, une capture au chargement précède le contenu asynchrone, c'est ce qui avait masqué les deux défauts. Palette par défaut : **Poste 1 / Poste 2** (les postes réels, clé de stockage versionnée) ; l'ajout inline devient la fenêtre **⚙ Gestion des postes**. Recette : douze gestes. | IHM du banc |
| 2026-08-27 | **IHM du banc, itération 2** sur directives client : thème blanc/noir/**bleu outremer**, logo AIO, la journée en **frise horizontale** 00h–24h avec curseur d'heure courante. **Pose de départ au drapeau** : choisir un poste, prendre le drapeau, cliquer la frise (pas de 5 min, fantôme sous la souris, refus des heures passées), le drapeau posé est un départ à **date unique** (`debut=fin=jour`), la frise représentant la journée et non une récurrence. Retrait au clic avec confirmation explicite si l'entrée est récurrente. **Logs des départs** : ✓ confirmés avec heure prévue, ✗ sautés avec motif. **Bouton d'appel** : `POST /api/appel`, mission immédiate **hors validation**, c'est un geste opérateur, pas un déclenchement autonome, la décision est documentée. Nouvelle route `GET /api/planning/jour` : occurrences d'aujourd'hui **passées comprises**, calculées par le moteur (DST compris) plutôt que recalculées en JavaScript. L'éditeur JSON brut disparaît de la page (l'API PUT reste, testée). 12 tests de contrat. | banc, serveur, 44 tests |
| 2026-08-27 | **`DEPLOY.md` manquants ajoutés**, la convention du dépôt (un `README.md` **et** un `DEPLOY.md` par dossier livrable) n'avait pas été appliquée au chantier Timer. `banc_api/DEPLOY.md` : construction, lancement, recette en neuf gestes, vérification en ligne de commande, limites assumées et diagnostic. `Timer/DEPLOY.md` : le chemin ordonné vers la mise en service, dont une **phase 0 de verrous non logiciels** (sécurité machine ISO 3691-4 et sélecteur physique, mesure de `t_setup`, capacité I²C) et une recette sur chariot. **Cause corrigée** : `ORGANISATION.md` vivait dans `Comm distance/docs/` et ne couvrait pas le dépôt unifié, remonté à `docs/` racine, élargi aux chantiers, et la paire `README`+`DEPLOY` y est désormais explicitement obligatoire, **même quand rien n'est déployable**. | conventions, 8 dossiers conformes |
| 2026-08-27 | **Banc local de l'API** (`banc_api/`) : le VRAI moteur derrière les routes de la spec §6 (pas une doublure) avec horloge simulée pilotable (`/api/sim/*`, absent de la cible) : se placer à 05:59, accélérer ×600, franchir minuit, simuler une heure douteuse. IHM avec bandeau de validation non contournable, prochaines occurrences, missions, journal motivé. **Codec JSON strict** du document (clé inconnue = erreur : une faute de frappe ignorée deviendrait une mission mal paramétrée), 13 tests de rejet/aller-retour, il servira tel quel à la persistance §3.3. **10 tests de contrat** (subprocess + HTTP réel) : verrou optimiste 428/409, mission à l'heure une seule fois, expiration de la validation à minuit, gel §2.3, révocation. **Décision** : toute modification du planning (PUT) révoque la validation du jour, ce qui a été validé n'est plus ce qui est en mémoire. ⚠️ Piège documenté : l'horloge simulée ne va que vers le futur, revenir en arrière ne dé-consomme rien. | banc, moteur, 42 tests |
| 2026-08-27 | **Moteur d'ordonnancement écrit et testé**, occurrences (masque de jours, validité, exceptions), validation quotidienne, grâce 5 min sans rejeu au boot, idempotence `(id, date locale)` couvrant le 02:30 double d'automne, DST printemps configurable (défaut : exécution au premier instant), gel sur heure non fiable, pause/saut/priorités, journal motivé, liste des prochaines occurrences. Un point de conception attrapé par les tests : le balayage de la veille (grâce à cheval sur minuit) journalisait comme « sautées » des missions d'hier qu'un redémarrage aurait pu avoir déjà exécutées, **le journal aurait menti**. La veille n'est plus balayée que dans la fenêtre de grâce ; le reste attend la persistance des clés (T1). | moteur, 18 tests |
| 2026-08-27 | Spec confrontée au dépôt : le séquenceur du §5 **existe déjà** (A3), pas de MCP23017 sur la V5.0.1 relevée, stations 0–1023. Voir `ALIGNEMENT_COMM_DISTANCE.md`. | périmètre réduit |
